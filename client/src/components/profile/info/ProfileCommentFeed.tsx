import { User } from "../../../graphql/types"

type ProfileCommentFeedProps = {
  user: User
}

const ProfileCommentFeed = ({ user }: ProfileCommentFeedProps) => {
  return <div>ProfileCommentFeed</div>
}

export default ProfileCommentFeed
