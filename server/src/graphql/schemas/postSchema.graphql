scalar DateTime

type Post {
  id: ID!
  body: String!
  created_at: DateTime!
  updated_at: DateTime!

  owner: User!
  community: Community!
}

# -- PAGINATION --
type PostEdge {
  node: Post!
  cursor: String!
}

type PostConnection {
  edges: [PostEdge!]!
  pageInfo: PageInfo!
}
