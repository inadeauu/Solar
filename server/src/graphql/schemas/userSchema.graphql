scalar DateTime

enum Provider {
  EMAIL
  GOOGLE
  GITHUB
}

type User {
  id: String!
  email: String!
  email_verified: Boolean!
  provider: Provider!
  image: String
  created_at: DateTime!
  updated_at: DateTime!
}
